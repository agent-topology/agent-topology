# v0.1.0-beta.3 release closeout candidate

Status: **Registry artifacts verified; coordinated release not published.** All four
registry packages and the immutable tag exist. Publication closeout is now resumed:
the repository installation guides still select beta.2 and the GitHub prerelease is
not public until the staged draft and closeout checks succeed. Retained evidence is in
[`evidence/v0.1.0-beta.3.json`](evidence/v0.1.0-beta.3.json).
The [beta.3 upgrade guide](../guides/upgrading-beta.3.md) remains labeled as not
published until that transition completes.

The frozen source and tag target are
`848b179aee32789a6f8b0ad4552a3a1262a05d55`, the merge of the beta.3
qualification work. The tag is retained and must never be moved. Finalization verifies
this target before it creates a draft GitHub prerelease.

## Selected packages

| Package | Registry version | Runtime | Framework |
| --- | --- | --- | --- |
| `agent-topology-spec` | `0.1.0b3` | Python 3.11–3.14 | None |
| `agent-topology-langgraph` | `0.1.0b3` | Python 3.11–3.14 | LangGraph 1.2.10–1.2.11 |
| `@agent-topology/spec` | `0.1.0-beta.3` | Node.js 20+ | None |
| `@agent-topology/langgraph` | `0.1.0-beta.3` | Node.js 20+ | LangGraph.js 1.4.14 |

The Python producer requires `agent-topology-spec>=0.1.0b3,<0.2.0`.
The npm producer's specification peer is `@agent-topology/spec@0.1.0-beta.3`.
The Python `agent-topology-langgraph` distribution alone owns `agt describe`.
The document format remains `0.1` and the structure-hash algorithm version
remains `1`. The four package versions were selected together for this coordinated
candidate; future releases remain independent.

**Live coordinated installation commands are unchanged.** The root README and
quickstarts continue to select verified beta.2 (`0.1.0b2` / `0.1.0-beta.2`) until the
dedicated published closeout PR lands. Package-source READMEs select the exact beta.3
candidate so future source builds and documentation checks cannot regress.

## Changes from beta.2

- Both specification packages now spell `x-*` extension numbers per
  [ADR 0009](../decisions/0009-numeric-canonical-form.md)'s byte oracle instead
  of each runtime's default float formatting, and both narrow a bare JSON
  integer literal beyond magnitude `2^53` to its nearest `binary64` double
  instead of rejecting it ([ADR 0010](../decisions/0010-numeric-domain-parsed-value-narrowing.md)).
  This changes full-document canonical bytes for documents with a numeric
  `x-*` extension value; it does not change `structureHash` (no core field is
  numeric). Do not assume full-document byte parity with beta.2 for a
  document that carries a numeric extension. See the
  [numeric canonical form section](../0.1-contract.md#numeric-canonical-form).
- Documented the explicit boundary between ordinary-edge connectivity and
  first-trigger/once-only/reset firing policy: no convergence firing-policy
  fact is added by experimental interpretation revision 1, and this is not an
  OR-convergence implementation. See the
  [consumer guide](../guides/consuming-documents.md#or-convergence-and-first-trigger-firing).
- Both LangGraph producers add experimental revision 1 `entry` facts
  distinguishing observed graph roots from confirmed execution entry points.
- Both LangGraph producers add experimental revision 1 sentinel roles from
  framework-owned identity and positive user-node membership.
- Both LangGraph producers add experimental revision 1 branch interpretation
  from inspected declarations.
- Every experimental extension above is additive: core nodes, edges, joins,
  `entryNodeIds`/`exitNodeIds`, completeness gaps, and `structureHash` are
  unchanged. Extension equality is not established by equal structure hashes.
- Beta.3 adds `derived_join_edges` (Python) and `derivedJoinEdges` (TypeScript)
  join-consumption helpers for already-validated structures. Coordinated beta.2
  packages do not export them. See the
  [join connections section](../guides/consuming-documents.md#join-connections).
- Producer-generated consumer fixtures now validate cross-language extension
  semantics and legacy/extension-unaware consumption against real Python and
  TypeScript producer output (F1–F6 disposition).
- Expanded ESM installation guidance is unchanged in substance from beta.2;
  see [troubleshooting](../guides/troubleshooting.md#esm-installation-errors).

See the [changelog](../../CHANGELOG.md#v010-beta3-release-closeout-2026-09-12)
for detailed evidence and links.

## Retained registry evidence

The [machine-readable evidence](evidence/v0.1.0-beta.3.json) is authoritative for
the complete URLs and provenance endpoints. The publication runs and immutable
artifact identities are:

- `agent-topology-spec==0.1.0b3`: [workflow run](https://github.com/agent-topology/agent-topology/actions/runs/34713578793);
  wheel SHA-256 `45732c5080126484af8ba4bea7a1767f72fca27721825c0f678321fa7093e276`;
  sdist SHA-256 `b6da5f5640eae25477771845ea171fef80195856163e69a5f9d4dc047862a71d`.
- `agent-topology-langgraph==0.1.0b3`: [workflow run](https://github.com/agent-topology/agent-topology/actions/runs/34713739005);
  wheel SHA-256 `c81658d8db2293c1a87255e7437d789592d40131bc5a6db2fc5b7412795312c6`;
  sdist SHA-256 `d434c07d48ab0b8e00563b623f305e1cf26dc09e3bdf4bb1e805dd5d804d426e`.
- `@agent-topology/spec@0.1.0-beta.3`: [workflow run](https://github.com/agent-topology/agent-topology/actions/runs/34713842862);
  tarball SHA-1 `05af1e343c7772fb96b5fcdc1229cd31b603f482`; integrity
  `sha512-URGMhakCLdA4wEbOWNjuZ0khJC8hiLLXZVtenSa0MmKKmY8W/LJYsrBx8tRykz6dzRSw/NjmbO9spjYB0JiZRQ==`.
- `@agent-topology/langgraph@0.1.0-beta.3`: [workflow run](https://github.com/agent-topology/agent-topology/actions/runs/34713955341);
  tarball SHA-1 `bbc99a3b996de10108aaa6fd273a614621e4e1cb`; integrity
  `sha512-f2XYPBkSnnH8wsJd2Sf6pB61Kf2zH4FfccCC0VuTm19VuSMQqgosgt+fgmn/SliGriGG9RZhGd4O1dFNxgbfiQ==`.

## Publication closeout status

- Package qualification and the four protected publication runs completed from the
  frozen source commit.
- Both PyPI wheels and source distributions and both npm tarballs are retained with
  their registry URLs and digests in the evidence record.
- `v0.1.0-beta.3` identifies the frozen source commit.
- The finalization stage must reverify the registries and create the draft GitHub
  prerelease, after which a dedicated PR moves every live installation path to beta.3.

The immutable artifacts and tag are never overwritten. Beta.3 becomes coordinated
only after the published documentation check passes and the finalization workflow
makes the prerelease public.
