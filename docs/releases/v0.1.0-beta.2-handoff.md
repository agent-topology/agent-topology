# beta.2 publication handoff

This record tracks [Task #84](https://github.com/agent-topology/agent-topology/issues/84).
It is not a publication announcement. The selected versions remain those in the
[candidate notes](v0.1.0-beta.2.md).

## Frozen source

The publication source is `cbd2f404a36834fb9ba318500b7832f03ba610da` on
`rc/0.1.0-beta.2`.
[PR #91](https://github.com/agent-topology/agent-topology/pull/91) synchronized
the already-reviewed npm dry-run fix from PR #90. Its 54 checks passed before
merge. The frozen tree is identical to main commit
`60d46b5532503aa77f3800a7f0d44a9408682ea0`.

Keep subsequent evidence and installation-guide changes on separate PR branches.
Do not advance the RC source during publication or move a version tag to a
documentation commit.

## Qualification evidence

All seven runs below succeeded on the frozen commit on 2026-09-11. All four
qualification runs used `publish=false`; each publication job was confirmed skipped.

| Check | Successful run |
| --- | --- |
| Python package CI | [34626817554](https://github.com/agent-topology/agent-topology/actions/runs/34626817554) |
| TypeScript package CI | [34626817509](https://github.com/agent-topology/agent-topology/actions/runs/34626817509) |
| Framework compatibility | [34626817519](https://github.com/agent-topology/agent-topology/actions/runs/34626817519) |
| Python spec qualification | [34626855944](https://github.com/agent-topology/agent-topology/actions/runs/34626855944) |
| Python producer qualification | [34626858913](https://github.com/agent-topology/agent-topology/actions/runs/34626858913) |
| npm spec qualification | [34626862266](https://github.com/agent-topology/agent-topology/actions/runs/34626862266) |
| npm producer qualification | [34626864821](https://github.com/agent-topology/agent-topology/actions/runs/34626864821) |

The workflows exercise package tests, artifact inspection and clean installation;
Python additionally covers namespace coexistence and Python 3.11–3.14.
Producer receipts include supported-framework conformance. The compatibility CI
covers Python LangGraph 1.2.10 and 1.2.11, and LangGraph.js 1.4.14.

The downloaded bundles were independently checked: all receipt commits and
versions match the selected source, and all six actual artifact SHA-256 digests
match the receipts. These are qualified artifacts, not public registry artifacts.

| Receipt | Artifact filename | SHA-256 |
| --- | --- | --- |
| [python-spec](evidence/beta.2/python-spec-qualification.json) | `agent_topology_spec-0.1.0b2-py3-none-any.whl` | `14aa6083e5d32dbec527ccb9422e91d99f4415c3d3b0a30d5677f108401a8873` |
| [python-spec](evidence/beta.2/python-spec-qualification.json) | `agent_topology_spec-0.1.0b2.tar.gz` | `b356c04be97915ec9ee7b9b57fb18bc762eec8f1ab37ecd593f9455474568193` |
| [python-langgraph](evidence/beta.2/python-langgraph-qualification.json) | `agent_topology_langgraph-0.1.0b2-py3-none-any.whl` | `a6926ef43af0a48a34ab2d29d24f3fa6d257bdd0aa0e84ee24897463ea009d16` |
| [python-langgraph](evidence/beta.2/python-langgraph-qualification.json) | `agent_topology_langgraph-0.1.0b2.tar.gz` | `69c4f5cdf80fed34539b86d5b7d86a5400648812555b1bcc6acabb84d6cf11a7` |
| [npm-spec](evidence/beta.2/npm-spec-qualification.json) | `agent-topology-spec-0.1.0-beta.2.tgz` | `373fbb08548ebcf8d62abb952b5f1e711be3d2616579cbe86e8c30f89e8aa9bc` |
| [npm-langgraph](evidence/beta.2/npm-langgraph-qualification.json) | `agent-topology-langgraph-0.1.0-beta.2.tgz` | `4a13dfe6b5044ae7754708dbdf30ff0cf9f7baccabeb543ed13677311f9daeb1` |

Full qualification bundles, raw receipts, run metadata and zipped workflow logs
are retained locally under `dist/beta.2-evidence/qualification/<package>/`.
The small raw receipts above are also committed so evidence survives workflow
artifact expiration. The workflow artifacts are named `authorized-python-release`
and `authorized-npm-release`.

## Approval boundary

On 2026-09-11, all three live environments (`pypi-agent-topology-spec`,
`pypi-agent-topology-langgraph`, and `npm-public-preview`) required reviewer
`milocosmopolitan` and allowed exactly `rc/*` branches, with no tag policies.
Self-review was allowed; administrator bypass remained configured but was not used.
No credential or registry configuration was changed.

The maintainer must approve each publication deployment in GitHub Actions after
reviewing its exact source and artifact evidence. Qualification alone does not
provide this approval.

## Ready for maintainer approval

Both spec publication runs have passed every prerequisite job on the frozen
commit and are waiting at their protected `publish` jobs:

| Package | Approval page | Publication receipt |
| --- | --- | --- |
| Python spec | [34627020832](https://github.com/agent-topology/agent-topology/actions/runs/34627020832) | [Exact receipt](evidence/beta.2/python-spec-publication.json) |
| npm spec | [34627023206](https://github.com/agent-topology/agent-topology/actions/runs/34627023206) | [Exact receipt](evidence/beta.2/npm-spec-publication.json) |

The maintainer's next action is **Review deployments → select the listed
environment → Approve and deploy** on each page. These actions publish only the
two specification packages. Both downloaded publication receipts are identical
to their qualification receipts, and the three rebuilt artifact digests match
the qualification table exactly. There are no rebuild differences to review for
these two runs. Bundles are retained under
`dist/beta.2-evidence/publication/<package>/`.

Producer publication has not been dispatched: its specification must first be
public and verified. After that, the operator can dispatch each producer with:

```bash
rtk proxy gh workflow run release-python.yml --repo agent-topology/agent-topology --ref rc/0.1.0-beta.2 -f package=langgraph -f version=0.1.0b2 -f publish=true
rtk proxy gh workflow run release-npm.yml --repo agent-topology/agent-topology --ref rc/0.1.0-beta.2 -f package=langgraph -f version=0.1.0-beta.2 -f spec-version=0.1.0-beta.2 -f publish=true
```

Before dispatch, confirm the remote RC still equals the frozen commit. Apply
the same receipt/digest comparison before approving each producer deployment.
The Python registry preflight runs inside the approved publish job; the npm
producer registry preflight runs before its approval gate. A successful approval
is therefore not itself a successful publication or installation result.

## Verification and remaining issue scope

Local verification passed: 46 Python producer tests, 114 combined specification,
schema, release-tool and documentation tests, 10 TypeScript release-tool tests,
and Ruff format/lint checks. The documentation guard was also rerun after adding
this record. The initial combined Python check used the wrong pytest entry point
and failed import collection; the repository's `python -m pytest` invocation
resolved it without a source change.

At handoff, registry publication, registry-only installation, public provenance,
the coordinated tag/prerelease, installation-guide promotion and final milestone
review remain pending. #84 is not complete. Public installation instructions
continue to select beta.1 until beta.2 is verified. This evidence branch should
be carried into the eventual release-record PR without advancing the frozen RC.

## Completion after approval

1. Publish each specification before dispatching its producer publication.
   Python versions are `0.1.0b2`; npm versions and the producer's exact spec peer
   are `0.1.0-beta.2`. The npm workflow derives dist-tag `beta` from that version.
2. Compare each publication receipt's commit, filename and SHA-256 with the
   qualification record. Download the publication bundle before approval;
   review any rebuild difference rather than silently accepting new bytes.
3. After each upload, query the exact public registry version, download its
   artifacts, and compare the bytes with the publication receipt. Retain registry
   metadata, workflow logs, npm provenance and PyPI provenance where available.
4. Install all exact package versions in clean environments using only public
   registries. Run `scripts/smoke_spec_install.py` for the standalone Python spec
   and `scripts/smoke_langgraph_installation.py` for the Python producer's API and
   `agt` command. For npm, exercise the documented spec utilities standalone and
   the producer API with its exact public spec peer, without local support packages.
5. Only after all four packages pass, record registry URLs, filenames, digests,
   provenance and installation logs in the candidate notes; create
   `v0.1.0-beta.2` at `cbd2f404a36834fb9ba318500b7832f03ba610da` and the GitHub
   prerelease. Update published installation guides and migration status through
   PRs, run documentation tests, and review the milestone outcome before closing #84.

If any upload fails or has an ambiguous outcome, first query the exact registry
version. Preserve successful immutable artifacts, record partial publication, and
follow [recovery guidance](../releasing.md#recovering-a-partial-publication).
Do not create the coordinated tag or prerelease while any package remains unverified.
