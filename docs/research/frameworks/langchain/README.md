# LangChain research

LangChain research is separated by implementation language and remains distinct
from LangGraph producer dependencies. It exists to document integration and
authoring surfaces that can produce compiled LangGraph objects, not to make
LangChain a required producer runtime dependency.
