# LangGraph Python 1.2.11 probes

This directory will contain minimum executable inputs for the exact
`langgraph==1.2.11` research baseline. Each probe must answer one introspection
question and record both builder state and public drawable output where they
can differ.

Probe code has not been added yet. Until a finding is reproduced here, its
catalog state remains `source-verified` rather than `verified`.
