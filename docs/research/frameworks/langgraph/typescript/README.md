# LangGraph TypeScript research

TypeScript producer research is deferred. Its package baseline, source commit,
API surfaces, and probes must be selected independently from Python and added
under a version directory such as `<version>/dossier.md`.

Do not infer TypeScript support from Python source findings or conformance
results. Only the fixture meanings are shared.
