# Git consumer recheck — 2026-09-17

[Repository](https://github.com/milocosmopolitan/git-agent) ·
[Snapshot and probe output](probe-2026-09-17.json) ·
[Release findings and plan](../pre-beta-review-2026-09-17/README.md)

Baseline: remote main / local HEAD
`b67cb35a6195609a5d89976768fb5a4884f17ca1`. No releases or tags existed at
inspection; no release tag can be peeled. A local README modification was observed
at final inspection and was not changed or used as committed evidence. Source/dev
files inspected by this review were unchanged.

## Implemented and reproduced

The [issue_resolution factory](https://github.com/milocosmopolitan/git-agent/blob/b67cb35a6195609a5d89976768fb5a4884f17ca1/src/git_agent/graphs/issue_resolution/graph.py#L114-L256)
is the only implemented product StateGraph found in the indexed source. Its
declared structure is flat: conditional routes, model self-retries, human approval,
effect nodes, and terminal paths. At depths 0/1/2, the current topology producer
returns 21 nodes including sentinels, 44 edges, no joins, and a valid complete
document. This verifies extraction, not the outcome of running its effect nodes.

The [dev adapter](https://github.com/milocosmopolitan/git-agent/blob/b67cb35a6195609a5d89976768fb5a4884f17ca1/dev/issue_resolution.py#L30-L79)
now exposes `graph = create_dev_graph()` as well as a factory. A new generic CLI
factory loader is unnecessary for this inspection surface. The probe imports this
real compiled object with `invoke`/`ainvoke` disabled and supplies the explicit
topology id `issue_resolution`. A compile display name alone still does not set
the producer's document-local id; its default remains `main`.

[Metadata](https://github.com/milocosmopolitan/git-agent/blob/b67cb35a6195609a5d89976768fb5a4884f17ca1/pyproject.toml)
now pins workflow-core commit `876f6a8a4ba863424e1f85bc92f18dce59147957`, replacing
the older report's beta.3 baseline. The existing consumer environment runs
LangGraph 1.2.11 and falls inside the producer's tested range.

## Source-verified boundary and unverified requirements

[human_approval](https://github.com/milocosmopolitan/git-agent/blob/b67cb35a6195609a5d89976768fb5a4884f17ca1/src/git_agent/graphs/issue_resolution/nodes.py#L1003-L1088)
uses dynamic interrupt/resume and validates plan/approval binding. The topology
contains the node and possible paths, but no static interrupt marker. No claim is
made about valid approval, successful commit/push/PR effects, trace graph/run
mapping, or repeated attempt identity. Those require a separate fake-runtime
integration with captured events. The current extraction itself is not blocked by
subgraph depth. Approved or draft future graphs are not counted as implemented.

The [older report](README.md) remains historical. Generic topology/core probes do
not prove git-agent's own runtime integration; the real-factory extraction above
closes only its compile/export and structural representation questions.
